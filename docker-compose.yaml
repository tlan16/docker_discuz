version: '3.3'
services:
  app:
    build: .
    ports:
      - 8080:80
    depends_on:
      - db
  db:
    image: mysql
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: ultrax
